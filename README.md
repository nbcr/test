# Math Test 2

Inline: $E=mc^2$ (should be inline)

Display with `$$`:
$$
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
$$

Display with `math` code block:

```math
\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}
```